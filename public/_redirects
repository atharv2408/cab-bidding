# Netlify redirects for React Router
# Static assets should be served normally
/static/* /static/:splat 200

# Handle specific driver portal routes
/driver/login /index.html 200
/driver/dashboard /index.html 200
/driver/active-rides /index.html 200
/driver/history /index.html 200

# Handle all other routes with React Router (but not static files)
/* /index.html 200